function analyzeData(filename)

data = csvread(filename);  %loads data into an array
%Each row corresponds to a single simulation, with the s-amplitudes,
%according to [0,0],[0,-1],[-1,0],[1,0],[0,1]
%followed by p-amplitudes according to [0,0],[0,-1],[-1,0],[1,0],[0,1]

E1s = data(:,1)+i*data(:,2);
E1s = data(:,1)+i*data(:,2);


end